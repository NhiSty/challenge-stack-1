{% extends 'base.html.twig' %}

{% block title %}Demand index{% endblock %}

{% block body %}
    <h1>Demand index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>State</th>
                <th>FileNames</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for demand in demands %}
            <tr>
                <td>{{ demand.id }}</td>
                <td>{{ demand.state ? 'Yes' : 'No' }}</td>
                <td>{{ demand.fileNames ? demand.fileNames|join(', ') : '' }}</td>
                <td>
                    <a href="{{ path('admin_app_demand_show', {'id': demand.id}) }}">show</a>
                    <a href="{{ path('admin_app_demand_edit', {'id': demand.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('admin_app_demand_new') }}">Create new</a>
{% endblock %}
