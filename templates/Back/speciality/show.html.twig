{% extends 'base.html.twig' %}

{% block title %}{{ speciality.name }}{% endblock %}

{% block body %}

    <div class="sm:max-w-md mx-auto bg-white sm:rounded-lg overflow-hidden shadow-lg my-0 sm:my-6">
        <div class="p-4 bg-gray-800 text-white">
            <h1 class="text-2xl font-bold">{{ speciality.name }}</h1>
        </div>
        <div class="p-4">
            <div class="mb-4">
                <a href="{{ path('admin_app_speciality_index') }}" class="gradient px-4 py-2 rounded-lg text-white font-bold create-new">
                    <i class="fas fa-arrow-left mr-2"></i>
                    {{ 'Retour à la liste'|trans }}
                </a>
            </div>
            <table class="table-auto w-full">
                <tbody>
                <tr>
                    <th class="border px-4 py-2">Id</th>
                    <td class="border px-4 py-2">{{ speciality.id }}</td>
                </tr>
                <tr>
                    <th class="border px-4 py-2">Name</th>
                    <td class="border px-4 py-2">{{ speciality.name }}</td>
                </tr>
                <tr>
                    <th class="border px-4 py-2">Description</th>
                    <td class="border px-4 py-2">{{ speciality.description }}</td>
                </tr>
                </tbody>
            </table>
            <div class="flex justify-between mt-4 flex-col sm:flex-row">
                <div class="flex justify-between gap-2">
                    {{ include('/Back/speciality/_delete_form.html.twig') }}
                    <a class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600" href="{{ path('admin_app_speciality_edit', {'id': speciality.id}) }}">Edit</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
