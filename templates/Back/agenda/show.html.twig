{% extends 'base.html.twig' %}

{% block title %}Agenda{% endblock %}

{% block body %}
    <h1>Agenda</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ agenda.id }}</td>
            </tr>
            <tr>
                <th>Availablity</th>
                <td>{{ agenda.availability ? agenda.availability|join(', ') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_app_agenda_index') }}">back to list</a>

    <a href="{{ path('admin_app_agenda_edit', {'id': agenda.id}) }}">edit</a>

    {{ include('/Back/agenda/_delete_form.html.twig') }}
{% endblock %}
