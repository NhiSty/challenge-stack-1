<form method="post" action="{{ path('admin_app_clinic_delete', {'id': clinic.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ clinic.id) }}">
    <button class="btn">Delete</button>
</form>
