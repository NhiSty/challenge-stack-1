{% extends 'base.html.twig' %}

{% block title %}Clinic{% endblock %}

{% block body %}
    <h1>Clinic</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ clinic.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ clinic.name }}</td>
            </tr>
            <tr>
                <th>Country</th>
                <td>{{ clinic.country }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ clinic.city }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ clinic.address }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_app_clinic_index') }}">back to list</a>

    <a href="{{ path('admin_app_clinic_edit', {'id': clinic.id}) }}">edit</a>

    {{ include('/Back/clinic/_delete_form.html.twig') }}
{% endblock %}
